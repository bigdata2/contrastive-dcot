# torch is intentionally excluded here — install via setup.sh to get the cu124 wheel.
# vllm is listed but must be installed before torch is re-pinned (see setup.sh).
vllm==0.6.3
transformers==4.46.3
accelerate==0.34.0
peft==0.13.2
trl==0.9.6
datasets==2.18.0
bitsandbytes==0.43.1
scikit-learn==1.4.2
nltk==3.8.1
evaluate==0.4.2
